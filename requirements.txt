# ─────────────────────────────────────────────────────────────────────────────
# Vizro Analytics — Runtime Dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Install all core deps:   pip install -r requirements.txt
# Install optional extras: pip install -r requirements.txt -r requirements-optional.txt
#
# For the Mac native app build, only CORE deps are bundled.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core: Data & Analytics ────────────────────────────────────────────────────
pandas>=1.5.0
numpy>=1.21.0
scipy>=1.9.0
scikit-learn>=1.0.0
statsmodels>=0.13.0
networkx>=2.8.0
python-dateutil>=2.8.0
pytz>=2022.1

# ── Core: Visualization ───────────────────────────────────────────────────────
plotly>=5.0.0

# ── Core: Causal Analysis ─────────────────────────────────────────────────────
causalnex>=0.12.0

# ── Core: File Formats ────────────────────────────────────────────────────────
openpyxl>=3.0.0          # Excel .xlsx read/write
xlrd>=2.0.0              # Excel .xls read (legacy)
requests>=2.28.0         # URL data loading

# ── Core: Web Server (FastAPI backend for native app) ─────────────────────────
fastapi>=0.95.0
uvicorn>=0.22.0
python-multipart>=0.0.6  # FastAPI file upload support

# ── Core: Mac Native Window ───────────────────────────────────────────────────
pywebview>=4.0.0

# ── Web UI (Gradio dashboard — NOT bundled in native app) ─────────────────────
# Uncomment when running in web/api mode:
gradio>=4.0.0,<6.0.0

# ─────────────────────────────────────────────────────────────────────────────
# OPTIONAL EXTRAS — See requirements-optional.txt for:
#   torch        (LSTM forecasting, ~276 MB)
#   pyarrow      (Parquet file format, ~114 MB)
#   matplotlib   (extra chart types)
#   seaborn      (statistical plots)
# ─────────────────────────────────────────────────────────────────────────────
